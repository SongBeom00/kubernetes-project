apiVersion: v1
kind: Pod

metadata:
  name: spring-pod-1

spec:
  containers:
    - name: spring-container
      image: spring-server
      ports:
        - containerPort: 8080 # 다른 개발자들이 알기 위해 명시적으로 적어줍니다.
      imagePullPolicy: IfNotPresent

---
apiVersion: v1
kind: Pod

metadata:
  name: spring-pod-2

spec:
  containers:
    - name: spring-container
      image: spring-server
      ports:
        - containerPort: 8080 # 다른 개발자들이 알기 위해 명시적으로 적어줍니다.
      imagePullPolicy: IfNotPresent

---
apiVersion: v1
kind: Pod

metadata:
  name: spring-pod-3

spec:
  containers:
    - name: spring-container
      image: spring-server
      ports:
        - containerPort: 8080 # 다른 개발자들이 알기 위해 명시적으로 적어줍니다.
      imagePullPolicy: IfNotPresent

